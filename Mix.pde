/**
 * A container for a node and its father.
 */
public class Mix {
  Tree node;
  Tree father;
  
  public Mix(Tree node, Tree father) {
    this.node = node;
    this.father = father;
  }
  
}
